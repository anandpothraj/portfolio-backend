const express = require("express");
const router = express.Router();

router.get("/fetchSiteVisits", (req, res) => {
  try { 
    
  }
  catch (e) {
  }
});

router.post("/updateSiteVisits", (req, res) => {
  try { 
  }
  catch (e) {
  }
});

module.exports = router;